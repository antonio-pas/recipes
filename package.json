{
  "name": "foods",
  "scripts": {
    "watch": "esbuild app.jsx --outfile=dist/output.js --bundle --sourcemap --jsx-factory=h --jsx-fragment=Fragment --watch",
    "twwatch": "tailwindcss -i input.css -o dist/output.css --watch"
  },
  "devDependencies": {
    "esbuild": "^0.14.36",
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {
    "preact": "^10.7.1"
  }
}
